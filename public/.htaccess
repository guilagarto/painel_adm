<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Se o arquivo ou pasta requisitada fisicamente não existir, joga pro index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [L,QSA]
</IfModule>
