<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redireciona para a pasta public mantendo a URL limpa
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>

# Bloqueia listagem de pastas por segurança
Options -Indexes
