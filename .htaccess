<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redireciona invisivelmente todo o tráfego da raiz para a pasta public
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
